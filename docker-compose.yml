services:
  webproga:
    build: .
    container_name: webproga_app
    restart: always
    ports:
      - "127.0.0.1:3000:3000"
    networks:
      - aiphra-net

networks:
  local:
    external: true
